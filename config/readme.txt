/* This directory contains three configuration files.
   They are read by your program every time it is submitted.

   autoexec.sas               -> project specific autoexec
   options.sas                -> sas global options
   global_macro_variables.sas -> global macro variables

*/
